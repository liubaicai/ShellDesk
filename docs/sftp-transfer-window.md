# SFTP 文件传输窗口

ShellDesk 提供独立的双栏 SFTP 文件传输窗口。入口位于主机卡片的三点菜单和主机列表操作栏；它复用主机的认证信息，但不会先打开远程桌面。

## 界面结构

- 顶部会话栏显示主机、端口、用户、协议状态和连接安全状态。
- 左栏浏览本地文件系统，右栏浏览远端 SFTP 文件系统；两侧目录树支持按三角按钮懒加载展开/收起子目录，点击目录名称才进入该目录，并都有独立的新建文件/目录入口、路径历史、上级目录、刷新、搜索、排序、多选和右键菜单。
- 设置页可分别配置新窗口的本地和远端起始目录；配置在窗口创建时读取一次，不会在浏览过程中重置当前路径。默认本地 `/` 在 Windows 上显示磁盘列表，默认远端 `.` 由服务器解析为登录用户主目录。
- 中间操作栏根据当前选择执行上传或下载；未选择条目时传输当前目录的全部内容，并提供递归目录对比和单向同步入口。
- 顶部“传输队列”按钮可显示或隐藏底部队列；隐藏时双栏文件区域自动填满释放出的空间，新任务入队时会自动恢复显示。队列支持 1–4 个并发任务、真实字节进度、文件完成数、速度、剩余时间、暂停、继续、取消、重试和清理已完成任务。
- 应用标题栏提供全局传输中心，可跨主窗口和 SFTP 窗口查看正在传输、已完成、失败和取消的任务，展开来源/目标路径，取消活动任务，并单独移除或批量清理已结束记录。
- 队列在创建时会整批提交给 Rust 后台传输运行时；排队与活动任务不依赖 React 组件存活，隐藏队列或关闭 SFTP 窗口后仍会按所选 1–4 并发度继续。窗口关闭时连接和临时密钥会延迟到最后一个后台任务结束后再清理。
- 重命名、删除和属性修改使用自定义对话框；远端属性对话框支持八进制权限和递归修改。
- 深色和浅色主题均有完整样式，窄窗口会收起目录树以保证文件表格可用。

设计基准见 [SFTP 文件传输窗口概念图](design/sftp-transfer-window-concept.png)。

## 后端边界

远端操作由 `src-tauri/src/remote_fs/sftp.rs` 实现：

- 使用 `russh_client::connect_authenticated` 建立并认证 russh 会话；
- 打开 SSH session channel 并请求 `sftp` subsystem；
- 使用 `russh-sftp` 完成目录、元数据、增删改、权限和文件流操作；
- 使用 256 KiB 缓冲区流式上传/下载，不把整个文件读入内存；
- 复用连接级传输进度与取消机制。

此窗口不调用系统 `ssh`、`sftp`、`sshpass` 或 OpenSSH fallback，也不通过 `exec` 命令模拟普通 SFTP 操作。

## IPC

远端通道统一以 `connection:sftp-` 开头，包括目录读取、状态、新建、删除、重命名、权限、上传和下载。本地双栏操作通过 `files:list-local-directory`、`files:stat-local-path` 和相应的增删改通道进入现有 `local_fs.rs`。

全局传输记录由 Rust 侧 `TransferHistory` 维护，并使用应用数据目录中的私有 JSON 文件持久化。进度和结束事件通过 Tauri 应用句柄广播到所有窗口；异常退出时遗留的 `queued` / `running` 记录会在下次启动时标记为失败，避免继续显示成活动任务。历史最多保留 200 条已结束记录，活动任务另计且不能从记录中移除。

`connection:sftp-enqueue-transfers` 会先验证整批任务、登记活动任务和排队历史，再启动进程级后台线程及独立 Tokio 运行时。该运行时继续复用既有 `russh-sftp` 上传/下载实现和取消标记，不会因为发起 IPC 的 WebView 被销毁而丢失尚未开始的任务。

新增或修改通道时必须同步：

1. Rust dispatcher；
2. `src/tauriBridge.ts`；
3. `src/vite-env.d.ts`；
4. React 调用与错误文案。

## 传输语义

- 目录上传与下载会递归创建目录并逐文件流式复制。
- 暂停运行中的任务会安全停止当前传输；继续或重试时，仅当来源路径、大小和修改时间组成的元数据指纹仍一致，并且来源与临时文件的完整已有前缀 SHA-256 相同，才会从临时文件的已验证字节偏移继续。指纹、临时文件大小或前缀摘要不匹配时会清空断点并从头传输。
- 下载先写入目标目录中的隐藏 `.shelldesk.part` 文件，并用相邻私有 JSON 检查点记录远端来源指纹；完成后再次校验远端来源，再通过平台原子替换把临时文件切换为正式目标。上传使用包含来源指纹摘要的远端隐藏临时文件，完成后重新校验本地来源；服务端允许覆盖式 rename 时直接原子替换，否则先把旧目标改名为备份，替换失败会回滚旧文件。
- 传输队列提供持久化的“自适应 / 兼容”模式。自适应模式使用 256 KiB 请求和单文件 16 路待确认写入管线，并按平均文件大小把文件并发限制在 2、4 或 8，单任务最坏管线缓冲预算为 32 MiB；服务端声明 `limits@openssh.com` 时 `russh-sftp` 仍会自动收紧请求大小。兼容模式固定为 32 KiB 请求、单任务、单文件、单待确认写入，供旧版或不稳定服务端使用。
- Rust 测试包含严格 40 KiB 服务端包长的兼容性传输、两种模式的完整内容校验和代表性工作负载请求数对比。需要本地重复测量协议管线开销时运行 `cargo test --manifest-path src-tauri/Cargo.toml sftp_profile_benchmark -- --ignored --nocapture`；该结果只用于版本间相对比较，不代替真实 SSH 网络基准。
- 上传或下载前会检查目标当前目录的同名项目，并用自定义弹窗让用户选择“覆盖 / 合并”“跳过已存在项”或“取消”：同名文件覆盖，同名文件夹合并并覆盖其中的同名文件；选择跳过时仍会递归遍历同名文件夹，只跳过目标中已经存在的子文件或类型冲突子目录，其他内容继续传输；文件与文件夹类型不一致时会先明确提示，确认覆盖后再删除旧目标。删除目录会递归处理。
- 目录对比会在单个 SFTP 会话中递归枚举两侧目录树，以相对路径、条目类型和文件大小统计全部差异；单向同步会把包含差异的顶层文件或目录加入传输队列，不自动删除目标侧独有文件。

## 验证

```bash
pnpm typecheck
pnpm check:contracts
cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo check --manifest-path src-tauri/Cargo.toml
```

如仓库根目录存在完整的 `.env` 测试 SSH 变量，可再运行 live SSH 验证；除主机、端口、用户和认证信息外，测试还需要 `SHELLDESK_TEST_SSH_KNOWN_HOSTS_PATH`。不要在日志或回复中输出测试密码；缺少凭据或 known_hosts 路径时应明确跳过，而不是构造测试主机。
